import { gameReactUtilFormatters } from './game-react-util-formatters';

describe('gameReactUtilFormatters', () => {
  it('should work', () => {
    expect(gameReactUtilFormatters()).toEqual('game-react-util-formatters');
  });
});
